# PixCheese

像素吐司客户页。

访问链路：

客户页 -> EMQX `pixcheese/...` topic -> `tusi-emqx` 中间层 -> 内网 tusi 接码接口。

客户页不直接访问 tusi 服务器。

根地址只显示 404。吐司后台生成的专属结尾地址会加载客户页，页面从地址结尾读取内部卡密并通过 EMQX 自动核对；核对失败继续显示 404，核对通过后才显示取号和查码操作。

后台可生成 1 次或 2 次专属地址。收码成功后服务器释放并拉黑该号码；当前页面继续显示本次手机号和验证码，刷新或重新打开后按剩余次数决定是否继续使用。
